<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="ToolbarStyle" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <item name="colorControlNormal">@color/accent</item>
    </style>

    <style name="ToolbarViewStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@color/white</item>
        <item name="android:theme">@style/ToolbarStyle</item>
        <item name="popupTheme">@style/Theme.AppCompat</item>
    </style>

  <!--  <style name="infoButtonStyle">
        <item name="gravity">start</item>
        <item name="textAlignment">viewStart</item>
        <item name="textAllCaps">false</item>
        <item name="textColor">@color/white_transparent_12</item>
        <item name="textSize">@dimen/text_medium</item>
    </style>-->

</resources>
