package com.niedzwiecki.przemyslguide.ui.maps;

import com.google.android.gms.maps.SupportMapFragment;

public class MapFragmentWrapper extends SupportMapFragment {

/*
    public View originalContentView;
    public TouchableWrapper touchView;

    @Override
    public View onCreateView(LayoutInflater inflater, ViewGroup parent, Bundle savedInstanceState) {
        originalContentView = super.onCreateView(inflater, parent, savedInstanceState);
        touchView = new TouchableWrapper(getActivity());
        touchView.addView(originalContentView);
        return touchView;
    }

    @Override
    public View getView() {
        return originalContentView;
    }

    @Override
    public void onDestroy() {
        super.onDestroy();
        touchView.removeView(originalContentView);
        originalContentView = null;
        touchView = null;
    }
*/
}
